---
- name: My First Play Exercise 3a
  hosts: localhost
  gather_facts: False
  tasks:
    - ansible.builtin.debug: 
        var: ansible_host

- name: My Second Play Exercise 3c
  hosts: cisco
  gather_facts: False
  tasks:
    - ansible.builtin.debug:
        var: ansible_connection

    - ansible.builtin.debug:
        var: ansible_host
 

