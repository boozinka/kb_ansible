{% for vrf_dict in vrfs_list %}
ip vrf {{ vrf_dict.vrf_name }}
 rd {{ vrf_dict.rd_num }}
!
{% for intf in vrf_dict.interfaces %}
interface {{ intf }}
  ip vrf forwarding {{ vrf_dict.vrf_name }}
!
{% endfor %}
{% endfor %}

